# migration backstop — 1.7 GB rsync of /wp-content/uploads, not needed by the
# build (post images are co-located under output/posts/*/images). Kept locally
# and on the droplet as the image source-of-truth.
wp-uploads/

# build output
dist/
.astro/

# dependencies
node_modules/

# pagefind index (generated into dist)
**/pagefind/

# env / local
.env
.env.*
.DS_Store

# logs
npm-debug.log*
